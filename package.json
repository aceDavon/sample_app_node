{
  "name": "sample-app",
  "version": "1.0.0",
  "description": "Minimal Node.js HTTP server — used to test the Brimble deployment pipeline",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
